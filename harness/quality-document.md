# 质量文档

每个产品领域和架构层的质量快照。agent 和人都能通过这份文档快速了解项目哪里强、哪里弱。

**更新时机：** 每轮重要会话结束后，或开始新一阶段工作前。

**评级标准：**

- **A**：验证全部通过，架构干净，agent 能读懂，测试稳定
- **B**：验证通过，基本干净，可读性或测试覆盖有少量缺口
- **C**：部分可用，有已知缺口，部分代码 agent 不容易理解
- **D**：不可用，或存在重大结构问题

---

## 产品领域

| 领域 | 评级 | 验证状态 | Agent 可读性 | 测试稳定性 | 关键缺口 | 上次更新 |
|------|------|---------|-------------|-----------|---------|---------|
| 本地账户与建档 | B | smoke + 微信开发者工具建档点击通过 | 清楚 | 稳定 | 已接云托管账户同步代码，尚未部署验证 | 2026-06-12 |
| 云端账户与数据同步 | B | smoke + Go 单测通过；清缓存测试暴露本地 id 恢复缺口，代码已补 wx.login/sessionToken 身份链路和 local 到 openid 数据迁移 | 清楚 | 稳定 | 需部署并配置 AppSecret 后复测清缓存恢复；DevTools 仍有 wx.login timeout 噪声 | 2026-06-12 |
| 热量目标计算 | B | smoke 通过 | 清楚 | 稳定 | 公式为初版估算，未做个性化调参 | 2026-06-12 |
| 活力橙 UI 复刻 | B | smoke + 微信开发者工具首页/记录页固定视口验证通过 | 清楚 | 稳定 | 尚未做真机或像素级截图比对 | 2026-06-12 |
| 餐食记录 | B | smoke + 微信开发者工具点击流 + 云端保存联调通过 | 清楚 | 稳定 | 尚未验证清缓存后的云端恢复 | 2026-06-12 |
| 热量估算 | B | smoke 通过 | 清楚 | 稳定 | 内置食物库很小，未接外部食物库或图片识别 | 2026-06-12 |
| 每日达标判断 | B | smoke + 微信开发者工具首页汇总通过 | 清楚 | 稳定 | 首页未展示更明确的超标/达标文案 | 2026-06-12 |

## 架构层

| 层级 | 评级 | 边界执行 | Agent 可读性 | 关键缺口 | 上次更新 |
|------|------|---------|-------------|---------|---------|
| 小程序配置 | B | `app.json` 页面路径、窗口配置和按需注入配置清楚 | 清楚 | 最终上传需要用户确认版本号和描述 | 2026-06-12 |
| 页面层 | B | 页面负责输入收集、模板展示和跳转；记录页使用固定视口和内部滚动 | 清楚 | 未做真机或像素级截图比对 | 2026-06-12 |
| 业务工具层 | B | 账户、档案、热量计算、餐食记录与页面分离，云端调用集中在 `utils/cloud.js` | 清楚 | 云端失败重试/补偿队列还未实现 | 2026-06-12 |
| 本地存储 | B | 通过 storage API 封装账户、档案、餐食记录，云端不可用时可兜底 | 清楚 | 缺少离线队列和后台补同步机制 | 2026-06-12 |
| 云托管后端 | B | `server/` 内 Go HTTP 服务、MySQL store、Dockerfile、code2session 身份解析和单测边界清楚 | 清楚 | 新身份链路尚未部署到云托管 | 2026-06-12 |
| 验证层 | B | smoke 覆盖结构、语法、账户、建档、热量计算、餐食 CRUD、汇总和云端 wiring；Go 单测覆盖后端关键纯逻辑 | 清楚 | 缺少自动截图、真机验证和云端 E2E | 2026-06-12 |

## 变更历史

### 2026-06-12

- 变更内容：从空 harness 仓库启动原生微信小程序骨架，实现首次本地账户、身体信息建档、BMI 和每日建议热量展示。
- 提升：标准验证路径切换为 `./harness/init.sh` -> `npm test`，smoke test 可在无微信开发者工具时验证核心数据流。
- 下降：无。
- 新发现的缺口：尚未安装或调用微信开发者工具做人工预览；账户暂时只存在本地 storage。
- 已关闭的缺口：模板 chat 功能清单已替换为饮食热量管理小程序路线。

### 2026-06-12

- 变更内容：按 `style4-vibrant-orange.html` 复刻注册、首页、上传三页活力橙 UI。
- 提升：新增上传页；smoke test 增加模板关键文案和样式令牌检查。
- 下降：页面当前优先静态复刻模板，真实餐食记录交互仍未开始。
- 新发现的缺口：缺少微信开发者工具截图比对证据。
- 已关闭的缺口：此前绿色系自定义 UI 已替换为用户指定 HTML 模板风格。

### 2026-06-12

- 变更内容：修复微信开发者工具预览首页白屏问题；将 dashboard 头像的渐变样式从 WXML 内联 style 迁移到 WXSS `.avatar-bubble`。
- 提升：微信开发者工具确认 `pages/dashboard/index` 可渲染，点击底部加号可进入 `pages/upload/index`；smoke test 增加防回归检查，避免 dashboard WXML 再次出现内联 `linear-gradient`。
- 下降：无。
- 新发现的缺口：仍未做真机或像素级截图比对。
- 已关闭的缺口：首页白屏缺陷已关闭。

### 2026-06-12

- 变更内容：实现端到端餐食记录闭环；建档页、上传页输入框可编辑，首页按当天记录动态汇总。
- 提升：新增餐食后端工具 `utils/meal.js`，支持本地持久化、内置食物估算、CRUD 和每日汇总；微信开发者工具点击流已通过。
- 下降：无。
- 新发现的缺口：当前仍是本地 storage，未接微信云开发数据库；最终上传到微信后台需要用户确认版本号、上传描述和发布动作。
- 已关闭的缺口：餐食记录、基础热量估算、首页每日汇总从未开始提升为本地可用。

### 2026-06-12

- 变更内容：优化手机端固定视口和换行防护；记录页改为顶部信息 + 内部餐食列表滚动 + 底部固定总热量/保存按钮，首页餐食列表也改为内部滚动。
- 提升：微信开发者工具 iPhone 12/13 85% 验证首页和记录页不需要整页拖动；记录页主操作都在首屏；三位数 kcal 输入不截断；保存后可回到首页。
- 下降：无。
- 新发现的缺口：仍未做真机预览或像素级截图比对。
- 已关闭的缺口：记录页过长、食物行数字截断、首页到记录页叠层卡住、保存后回首页渲染滞后风险已关闭。

### 2026-06-12

- 变更内容：新增微信云托管 Go 后端和 MySQL 持久化；小程序端通过 `callContainer` 接入账户、档案、餐食保存和按日汇总读取。
- 提升：`./harness/init.sh` 现在同时运行 `npm test` 和 `server/go test ./...`；smoke test 增加云托管配置和前端调用入口检查。
- 下降：无。
- 新发现的缺口：云托管服务尚未部署，真实 MySQL 连通性和清缓存恢复仍未验证。
- 已关闭的缺口：云端方案已从“未确认”推进为“微信云托管 Go 服务 + MySQL 代码已实现并本地通过验证”。

### 2026-06-12

- 变更内容：云托管后端重新部署后完成公网 API 和微信开发者工具前后端联调；移除启动时未使用的 `wx.cloud.init()`，避免公网 HTTPS 主路径下的开发者工具 timeout 噪声。
- 提升：公网 `/healthz` 正常，`/api/meals/list` 空列表返回 `records:[]`；微信开发者工具保存默认午餐后首页展示 506 kcal，Network 面板请求 200，Console 无新 error。
- 下降：无。
- 新发现的缺口：清缓存恢复验证仍未执行；发布前还需要配置 request 合法域名。
- 已关闭的缺口：云端旧版本 `records:null` 与开发者工具启动 timeout 噪声已关闭。

### 2026-06-12

- 变更内容：清缓存验证暴露本地 account id 无法支持跨设备恢复；前端改为随云端请求附带 `wx.login` code，后端新增 `jscode2session` 换 openid 的身份链路，并签发短期 `sessionToken` 给后续请求复用；绑定 openid 时迁移旧 `local:*` 云端数据。
- 提升：身份设计从本地 id 回退升级为微信 openid 主键；smoke 和 Go 单测覆盖新环境变量、loginCode、code2session、sessionToken 签名校验和 local 到 openid 迁移入口。
- 下降：当前云托管运行版本尚未部署该修复，且需要配置小程序 AppSecret。
- 新发现的缺口：开发者工具中的 `callContainer` 仍会 timeout，不能作为本地调试主路径；基础库 3.16.1 仍会在 `wx.login`/身份请求附近出现 timeout，需要部署后端并配置 AppSecret 后复测。
- 已关闭的缺口：已明确本地 id 不是可靠恢复身份，并完成代码层修复。
