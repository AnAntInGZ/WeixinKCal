# 会话交接

## 当前已验证

- 现在明确可用的部分：原生微信小程序骨架、首次本地账户创建、身体信息建档、BMI/BMR/每日建议热量计算；建档页支持真实输入；首页、上传页按活力橙模板渲染；用户可以添加一餐并在首页看到当天热量汇总。
- 这轮实际跑过的验证：`npm test` 通过；`./harness/init.sh` 通过；微信开发者工具点击流通过：建档 -> 首页 -> 底部加号 -> 上传页 -> 保存 -> 首页显示午餐记录和 506 kcal 汇总。

## 本轮改动

- 新增了哪些代码或行为：新增 `utils/meal.js` 餐食后端工具；上传页支持日期、餐次、食物名、克重、每 100g 热量、备注、添加/删除食物和保存；首页读取当天餐食并动态汇总已摄入、剩余、进度和三大营养素。
- 基础设施或 harness 发生了哪些变化：`tests/smoke.js` 覆盖餐食新增、查询、汇总、更新、删除；`app.json` 增加 `lazyCodeLoading: requiredComponents`；`feature_list.json` 将 `wx-002`、`wx-003`、`wx-004` 标记为 passing，新增 `wx-006` 云端上传发布 blocked。

## 仍损坏或未验证

- 已知缺陷：暂无已复现缺陷。
- 未验证路径：未做真机或像素级截图比对；未执行微信开发者工具“上传”。
- 下一轮会话需要注意的风险：当前后端仍是本地 storage，不是云数据库；最终上传到微信后台会把代码提交到第三方平台，需要用户确认版本号、上传描述和最终动作；复杂渐变样式仍应放在 WXSS 中，不要写回 WXML 内联 style。

## 下一步最佳动作

- 最高优先级未完成功能：`wx-005` 升级账户为跨设备可恢复账户。
- 为什么它是下一步：本地餐食闭环已通，云端发布前最好先决定数据是否要接微信云开发数据库，否则用户换设备后无法恢复档案和餐食。
- 什么结果才算 passing：账户、档案和餐食记录可以在云端持久化；清空本地缓存后能按同一微信身份恢复；smoke 或开发者工具验证有证据。
- 这一步中哪些东西不要动：不要改弱 `tests/smoke.js`；不要把 `project.config.json` / `project.private.config.json` 的本地工具痕迹混入业务提交，除非明确是在配置正式 AppID 或云函数根目录。

## 命令

- 启动命令：`npm run dev`；实际预览需用微信开发者工具打开仓库根目录。
- 验证命令：`./harness/init.sh`
- 定向调试命令：`npm test`
