# 会话交接

## 当前已验证

- 现在明确可用的部分：原生微信小程序骨架、首次本地账户创建、身体信息建档、BMI/BMR/每日建议热量计算；建档页支持真实输入；首页、记录页按活力橙模板渲染；用户可以添加一餐并在首页看到当天热量汇总；首页和记录页在手机模拟器中不需要整页拖动。
- 这轮实际跑过的验证：`npm test` 通过；`./harness/init.sh` 通过；微信开发者工具 iPhone 12/13 85% 点击流通过：首页 -> 底部加号 -> 记录页 -> 保存 -> 首页。记录页主操作都在首屏，三位数 kcal 输入显示完整，保存后可回首页。

## 本轮改动

- 新增了哪些代码或行为：记录页改为固定视口 flex 布局，餐食列表使用内部 `scroll-view.food-list`，底部总热量和保存按钮固定在首屏；首页餐食列表使用内部 `scroll-view.meal-list`，长餐食详情单行省略；首页到记录页改为 `wx.redirectTo`，保存后回首页改为 `wx.reLaunch`。
- 基础设施或 harness 发生了哪些变化：`tests/smoke.js` 增加固定视口、内部滚动、nowrap、输入宽度和跳转方式防回归断言；`feature_list.json` 新增 `ui-002` 并标记 passing。

## 仍损坏或未验证

- 已知缺陷：暂无已复现缺陷。
- 未验证路径：未做真机或像素级截图比对；未执行微信开发者工具“上传”。
- 下一轮会话需要注意的风险：当前后端仍是本地 storage，不是云数据库；最终上传到微信后台会把代码提交到第三方平台，需要用户确认版本号、上传描述和最终动作；复杂渐变样式仍应放在 WXSS 中，不要写回 WXML 内联 style；微信开发者工具偶尔会有加载期白屏/截图滞后，验证时要等页面落稳。

## 下一步最佳动作

- 最高优先级未完成功能：`wx-005` 升级账户为跨设备可恢复账户。
- 为什么它是下一步：本地餐食闭环已通，云端发布前最好先决定数据是否要接微信云开发数据库，否则用户换设备后无法恢复档案和餐食。
- 什么结果才算 passing：账户、档案和餐食记录可以在云端持久化；清空本地缓存后能按同一微信身份恢复；smoke 或开发者工具验证有证据。
- 这一步中哪些东西不要动：不要改弱 `tests/smoke.js`；不要把 `project.config.json` / `project.private.config.json` 的本地工具痕迹混入业务提交，除非明确是在配置正式 AppID 或云函数根目录。

## 命令

- 启动命令：`npm run dev`；实际预览需用微信开发者工具打开仓库根目录。
- 验证命令：`./harness/init.sh`
- 定向调试命令：`npm test`
